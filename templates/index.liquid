<a>This is the list of all the Collections:</a>
<ul>
  {% for coll in collections %}
    <li>
      Elements of {{ coll.title }}:
      <ol>
        {% for prod in coll.products %}
          <li>{{ prod.title }}</li>
        {% endfor %}
      </ol>
    </li>
  {% endfor %}
</ul>
